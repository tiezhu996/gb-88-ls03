请根据以下要求生成项目代码和 README。

## ⚠️ 硬性约束（必须全部满足，否则视为项目不合格）

### 构建可运行性

- 前端执行 `npm install && npm run build`（或对应包管理器命令）必须零错误通过
- 后端执行编译命令（如 `go build`、`npm run build`、`python -m py_compile` 等）必须零错误通过
- 所有 import/require 的模块必须在 package.json / go.mod / requirements.txt 中声明
- TypeScript 项目不允许存在 any 类型逃逸导致的隐式运行时错误
- 数据库 Model/Schema 定义必须语法正确，ORM migration 必须能成功执行

### Docker 一键启动

- `docker compose build` 必须成功完成，无报错
- `docker compose up -d` 启动后所有容器状态为 healthy/running
- 浏览器访问前端地址能看到完整可交互的页面（不是空白页或报错页）
- 后端 API 的健康检查接口（如 /api/health）返回正常响应

### 功能实现完整性

- 列出的每一项功能必须有完整的前后端实现代码，不能只有页面路由或空壳组件
- 核心流程必须端到端跑通：从用户操作 → 前端请求 → 后端处理 → 数据库读写 → 返回结果 → 前端渲染
- 不允许出现"点击按钮无反应"、"表单提交后无数据保存"、"列表页面显示为空因为没有接口"等情况
- 前端页面必须有完整的 UI 布局和交互逻辑，不能是只有标题和占位文本的空白页面

### 代码质量

- 所有文件使用 UTF-8 编码
- 不得有未使用的 import 或声明后未使用的变量（会导致构建警告或报错）
- API 接口的请求参数和响应格式必须前后端一致（类型定义对齐）
- 错误处理：网络请求失败时前端有提示，后端有统一错误响应格式

## 项目主要需求

### 在线API Mock服务器

**项目概述：** 在线 API Mock 服务管理平台，通过配置 Mock 规则模拟 API 响应，加速前后端并行开发。

**技术要求：**

- 前端：Vue 3 + TypeScript + Arco Design + Vite
- 代码编辑器：Monaco Editor
- 后端：Node.js + Express + TypeScript
- 数据库：MongoDB + Mongoose
- Mock 引擎：Faker.js

**核心流程（必须端到端完整实现并可验证）：**

1. **创建 Mock 项目**：新建项目（如"用户服务"） → 生成项目 Base URL（如 `http://localhost:3119/mock/{projectId}`）
2. **配置接口**：在项目下添加接口 → 设置路径 `/api/users`、方法 GET、状态码 200 → 配置响应 Body（JSON）
3. **发送 Mock 请求**：GET `http://localhost:3119/mock/{projectId}/api/users` → 返回配置的 JSON 响应
4. **动态响应**：使用 Faker.js 模板 `{"name":"{{name.fullName}}","email":"{{internet.email}}"}` → 每次请求返回随机但真实的数据
5. **条件响应**：配置规则：当 query 参数 `role=admin` 返回管理员数据，否则返回普通用户数据
6. **请求日志**：查看所有发到 Mock 服务的请求记录（方法、路径、Headers、Body、时间）

**增强功能（在核心流程完成后实现）：**

1. Swagger 导入
2. 响应延迟模拟

**测试样例数据：**

- 预置 2 个用户：dev/dev123、lead/lead123
- 预置 1 个 Mock 项目「用户管理系统」包含 5 个接口：
  - GET /api/users → 返回用户列表（Faker.js 生成 5 个随机用户）
  - GET /api/users/:id → 返回单个用户详情
  - POST /api/users → 返回创建成功的用户（包含 id 和 createdAt）
  - PUT /api/users/:id → 返回更新成功的响应
  - DELETE /api/users/:id → 返回 204 No Content
- 预置 10 条请求日志记录

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面、组件、路由、状态管理、API 请求封装、类型定义、样式和资源等放在对应的子文件夹。

后端代码放在 `backend/`：
控制器/路由、服务层、数据模型、中间件、工具函数等按职责分层存放。

数据库脚本放在 `database/`：
包含建表 SQL 或 ORM migration 文件，以及种子数据（seed）脚本。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 端口映射（本项目分配的端口）：
  - 前端：8119:80
  - 后端：3119:3000
  - MongoDB：2919:27017
- 支持通过 `docker compose up -d` 一条命令启动全部服务

### 构建验证清单（生成代码后必须逐项验证）

1. `docker compose build` — 三个镜像全部构建成功，无报错
2. `docker compose up -d` — 所有容器启动，`docker compose ps` 显示 healthy/running
3. 浏览器访问 `http://localhost:8119` — 看到完整的应用首页
4. 前端页面上的核心功能按钮/链接全部可点击且有响应
5. 前端发起的 API 请求在浏览器 Network 面板中返回 200
6. 数据库中已有种子数据（见下方「测试样例数据」）

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- 测试账号（如果需要登录）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

